"first file" 

git clone "url"
-clones a git repo
git status
-gives status of local repo based on online repo

echo "first file" > first.txt
-makes first.txt file with "first file" stored in it

git add first.txt
-adds first.txt to online repo

git add -A 
-adds all

git commit -am "first meaningful commitment"
-commits and adds comment

git push origin master
-pushes to master branch

git config --list

git clean 
-    -nd shows what would be removed
-    -fd removes

git log 
-shows time and dates of git commits

git pull
-use to get repo from online
